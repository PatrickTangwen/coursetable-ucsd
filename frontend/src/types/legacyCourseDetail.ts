import type { CourseModalPrefetchListingDataFragment } from '../generated/graphql-types';

/** Inherited CourseTable/Yale Course Detail input. UCSD must not use it. */
export type LegacyCourseDetailListing = CourseModalPrefetchListingDataFragment;
